import { TheatreNamePipe } from './theatre-name.pipe';

describe('TheatreNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TheatreNamePipe();
    expect(pipe).toBeTruthy();
  });
});
